import 'package:flutter/material.dart';

class DealCancelledNotification extends StatelessWidget {
  const DealCancelledNotification({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
