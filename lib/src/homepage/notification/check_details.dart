import 'package:flutter/material.dart';

class Notification_details extends StatelessWidget {
  const Notification_details({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
